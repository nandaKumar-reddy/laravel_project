export default function UserManagements({ children }) {
    return (
        <div>
            <header>User Header</header>
            <main>{children}</main>
            <footer>User Footer</footer>
        </div>
    );
}
